#define cell_size 1000
#include <stdlib.h>
#include <stdio.h>
#include <time.h>

//This should be redone with dynamic resizing
int origin = cell_size/2;
int x = cell_size/2;
int y = cell_size/2;


int main(){
    int data[cell_size][cell_size] = {0};
    srand(time(NULL));

    %s //insertion point for generated code
return 0;
}
